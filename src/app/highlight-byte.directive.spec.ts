import { HighlightByteDirective } from './highlight-byte.directive';

describe('HighlightByteDirective', () => {
  it('should create an instance', () => {
    const directive = new HighlightByteDirective();
    expect(directive).toBeTruthy();
  });
});

import { CharFromCodePipe } from './char-from-code.pipe';

describe('CharFromCodePipe', () => {
  it('create an instance', () => {
    const pipe = new CharFromCodePipe();
    expect(pipe).toBeTruthy();
  });
});

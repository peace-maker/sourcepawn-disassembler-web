import { CompressionTypeAsStringPipe } from './compression-type-as-string.pipe';

describe('CompressionTypeAsStringPipe', () => {
  it('create an instance', () => {
    const pipe = new CompressionTypeAsStringPipe();
    expect(pipe).toBeTruthy();
  });
});

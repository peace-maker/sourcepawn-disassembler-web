import { SymScopeAsStringPipe } from './sym-scope-as-string.pipe';

describe('SymScopeAsStringPipe', () => {
  it('create an instance', () => {
    const pipe = new SymScopeAsStringPipe();
    expect(pipe).toBeTruthy();
  });
});
